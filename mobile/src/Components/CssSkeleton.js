import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container:{
    width: '100%',
    marginTop: 25,
    marginLeft: '5%',
    justifyContent: 'center',
    alignItems:'center'
  }
});